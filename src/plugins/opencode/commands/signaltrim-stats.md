---
description: Show signaltrim lifetime token-savings stats
---
Show signaltrim stats — total tokens saved, sessions, average compression ratio.

Read the lifetime history log at `~/.config/signaltrim/.signaltrim-history.jsonl`
(or wherever the signaltrim-stats script writes it). Output: total saved,
sessions counted, avg ratio. One short table.
